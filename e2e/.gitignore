# .yarn
.yarn/

# Appium drivers (installed via e2e/yarn setup)
.appium/

# Environment variables (local — not committed)
.env.e2e
.env.saucelabs

# Local app binaries
apps/*.app/
apps/*.apk
apps/*.ipa

# Logs
logs/

# Test reports (JUnit / Allure / failure screenshots)
reports/

# Downloaded CI artifacts + the rendered brief (yarn brief)
artifacts/
brief.md
brief.json

# Notes
.notes/

# Local environment variables
local.env

# Issuer provisioning output (ids only, no secrets)
issuer/.issuer-env