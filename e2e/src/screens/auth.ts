/**
 * Auth / unlock stack screen objects (v4.1 `AuthStack`). Populated by AUTH-1.
 * Add descriptors here (e.g. `EnterPINScreen`, `DeviceAuthScreen`) as `export const … = defineScreen(…)`.
 */
export {}
