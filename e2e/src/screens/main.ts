/**
 * Main stack screen objects (v4.1 `MainStack` / `TabStack` / `QRCoreStack`). Populated by MAIN-1..4.
 * Add descriptors here (e.g. `HomeScreen`, `TabBar`, `ServicesScreen`) as `export const … = defineScreen(…)`.
 */
export {}
