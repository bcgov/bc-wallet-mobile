/**
 * Verify stack screen objects (v4.1 `VerifyStack`). Populated by VFY-1..4.
 * Add descriptors here (e.g. `AccountSetupScreen`, `IdentitySelectionScreen`, `ScanSerialScreen`).
 *
 * Note: this stack is large (~20+ screens). If a single file becomes unwieldy or a merge-conflict
 * magnet during parallel VFY work, split it into a `verify/` folder — specs import from
 * `screens/verify.js`, so keep a barrel at that path to swap granularity without touching specs.
 */
export {}
