# BCSC Base - shared assets and deletions for all BCSC variants
# This is NOT a standalone variant. It provides shared overlay files (icons)
# and a common delete list consumed by all bcsc-* variants.

# Files to delete are listed in delete.txt
# Overlay files (icons, assets) are in overlay/
