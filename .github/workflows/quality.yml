name: Code Quality

on:
  push:
    branches: [main] # needed so codecov gets BASE coverage for comparison
    paths-ignore:
      - CHANGELOG.md
      - .changes/**
  pull_request:
    branches: [main, 'release/**']
    types: [opened, synchronize, reopened, labeled]
  merge_group:

jobs:
  # Mirrors main.yaml's early-exit-check: Build/Typecheck/Test/Coverage,
  # Lint & Format, and both Native Tests jobs are required checks, and a
  # workflow skipped entirely by a paths filter never reports them.
  # So the trigger stays unfiltered and this job decides
  # instead, letting the four heavy jobs report a fast "skipped" (which
  # satisfies a required check) rather than never running at all.
  detect-relevant-changes:
    name: Detect Relevant Changes
    runs-on: ubuntu-22.04
    permissions:
      contents: read
    outputs:
      should_skip: ${{ steps.check.outputs.should_skip }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Check if only CHANGELOG.md / .changes/** changed
        id: check
        shell: bash
        run: |
          set -x

          if [ "${GITHUB_EVENT_NAME}" = "merge_group" ]; then
            parent_ref="${{ github.event.merge_group.base_sha }}"
            child_ref="${{ github.event.merge_group.head_sha }}"
          elif [ "${GITHUB_EVENT_NAME}" = "push" ]; then
            parent_ref="${GITHUB_SHA}^"
            child_ref="${GITHUB_SHA}"
          else
            parent_ref="origin/${GITHUB_BASE_REF:-main}"
            child_ref="HEAD"
          fi

          echo "Comparing ${parent_ref} with ${child_ref}"

          diff_output=$(git diff --name-only "${parent_ref}".."${child_ref}")

          # A file is ignorable if it can only affect the generated
          # changelog, never behaviour a build, lint, or test pass would catch.
          ignorable() {
            local file="$1"
            [ "$file" = "CHANGELOG.md" ] && return 0
            [[ "$file" == .changes/* ]] && return 0
            return 1
          }

          relevant_count=0
          while IFS= read -r file; do
            [ -n "$file" ] || continue
            if ! ignorable "$file"; then
              echo "  relevant: $file"
              relevant_count=$((relevant_count + 1))
            fi
          done <<< "$diff_output"

          echo "$relevant_count relevant file(s) changed"

          if [ "$relevant_count" -eq 0 ]; then
            echo "should_skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_skip=false" >> "$GITHUB_OUTPUT"
          fi

  typecheck-test:
    runs-on: macos-26
    name: Build, Typecheck, Test & Coverage
    needs: detect-relevant-changes
    if: needs.detect-relevant-changes.outputs.should_skip != 'true'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      # Dependabot/fork runs get no repo secrets, so gate only the
      # secret-dependent steps below on this — build/typecheck/test stay
      # unconditional (job-level `if:` can't read `secrets`).
      - name: Check secret availability
        id: secrets-check
        if: always()
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
        run: |
          if [ -n "$OP_SERVICE_ACCOUNT_TOKEN" ]; then
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
            echo "OP_SERVICE_ACCOUNT_TOKEN unavailable (Dependabot or fork run); skipping 1Password load and codecov upload."
          fi

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - uses: ./.github/workflows/actions/setup-node

      - name: Cache node_modules
        id: cache-node
        uses: actions/cache@v6
        with:
          path: .yarn/cache
          key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: ${{ runner.os }}-yarn-

      - name: Install dependencies
        run: yarn install --immutable

      - name: Cache bcsc-core build output
        uses: actions/cache@v6
        with:
          path: packages/bcsc-core/lib
          key: ${{ runner.os }}-bcsc-core-build-${{ hashFiles('packages/bcsc-core/src/**') }}
          restore-keys: |
            ${{ runner.os }}-bcsc-core-build-

      - name: Build packages
        run: yarn build

      - name: Typecheck
        run: yarn typecheck

      - name: Run tests and generate coverage report
        run: yarn coverage

      # Secrets are being consolidated into the bcsc-mobile-app-cd 1Password vault;
      # OP_SERVICE_ACCOUNT_TOKEN is the single bootstrap secret that stays in GitHub.
      - name: Load secrets from 1Password
        uses: 1password/load-secrets-action@70062d7a876d3eb6334754fa26efd2fbd90c32f2 # v5.0.1
        with:
          export-env: true
        env:
          OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}
          CODECOV_TOKEN: op://bcsc-mobile-app-cd/codecov/credential
        if: always() && steps.secrets-check.outputs.available == 'true' && !startsWith(github.base_ref, 'release/') && !startsWith(github.ref_name, 'release/')

      - uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # (v7.0.0) pinning for SonarQube
        with:
          token: ${{ env.CODECOV_TOKEN }}
          fail_ci_if_error: true
          verbose: true
        if: always() && steps.secrets-check.outputs.available == 'true' && !startsWith(github.base_ref, 'release/') && !startsWith(github.ref_name, 'release/')

  lint-format:
    runs-on: macos-26
    name: Lint & Format
    needs: detect-relevant-changes
    if: needs.detect-relevant-changes.outputs.should_skip != 'true'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: '3.11'

      - uses: ./.github/workflows/actions/setup-node

      - name: Restore node_modules cache
        uses: actions/cache@v6
        with:
          path: .yarn/cache
          key: ${{ runner.os }}-yarn-${{ hashFiles('yarn.lock') }}
          restore-keys: ${{ runner.os }}-yarn-

      - name: Install dependencies
        run: yarn install --immutable

      - name: Install formatting and linting tools
        run: brew install swiftformat clang-format ktlint

      - name: Lint
        run: yarn lint

      - name: Format check
        run: yarn format:check

  test-ios:
    runs-on: macos-26
    name: Native Tests (iOS)
    needs: detect-relevant-changes
    if: needs.detect-relevant-changes.outputs.should_skip != 'true'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - name: Resolve iOS Simulator destination
        id: sim
        run: |
          SIM_NAME=$(xcrun simctl list devices available -j | python3 -c "
          import json, sys
          devices = json.load(sys.stdin)['devices']
          for runtime in sorted(devices.keys(), reverse=True):
            if 'iOS' in runtime:
              for d in devices[runtime]:
                if 'iPhone' in d['name']:
                  print(d['name']); sys.exit(0)
          sys.exit(1)")
          echo "destination=platform=iOS Simulator,name=$SIM_NAME,OS=latest" >> "$GITHUB_OUTPUT"

      - name: Run iOS tests
        working-directory: packages/bcsc-core
        run: |
          xcodebuild test \
            -scheme BcscCoreTests-Package \
            -destination '${{ steps.sim.outputs.destination }}' \
            -skipPackagePluginValidation

  test-android:
    runs-on: macos-26
    name: Native Tests (Android)
    needs: detect-relevant-changes
    if: needs.detect-relevant-changes.outputs.should_skip != 'true'
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v6
        with:
          distribution: zulu
          java-version: 17

      - name: Cache Gradle dependencies
        uses: actions/cache@v6
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('packages/bcsc-core/android/**/*.gradle*') }}
          restore-keys: ${{ runner.os }}-gradle-

      - name: Run Android tests
        working-directory: packages/bcsc-core/android
        run: ./gradlew test

  changelog-entry:
    name: Changelog Entry
    runs-on: ubuntu-22.04
    permissions:
      contents: read
    # Also runs on merge_group (required for ALLGREEN grouping in the merge
    # queue) but short-circuits to success there: PR labels aren't queryable
    # on merge_group, and a queued PR already passed this check as a
    # pull_request run before it could be queued.
    if: github.event_name == 'pull_request' || github.event_name == 'merge_group'
    steps:
      - uses: actions/checkout@v7
        if: github.event_name == 'pull_request'
        with:
          fetch-depth: 0

      - name: Verify a .changes/*.md file was added
        if: >-
          github.event_name == 'pull_request' &&
          !contains(github.event.pull_request.labels.*.name, 'status/skip-changelog')
        run: |
          added=$(git diff --diff-filter=A --name-only "origin/${GITHUB_BASE_REF}"...HEAD -- '.changes/*.md')
          if [ -z "$added" ]; then
            echo "::error::No .changes/*.md file added in this PR. Add one describing your change for a non-technical reader (see CONTRIBUTING.md), or apply the 'status/skip-changelog' label if this PR doesn't need one."
            exit 1
          fi
          echo "Found changelog entry: $added"
